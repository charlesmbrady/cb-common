{
  "name": "mockdat",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/mockdat",
  "projectType": "application",
  "tags": ["app", "next"],
  "targets": {
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "mockdat/jest.config.ts"
      }
    }
  }
}
