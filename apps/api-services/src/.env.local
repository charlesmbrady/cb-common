NODE_ENV=development
LOCAL_SERVER=true
PORT=3333

# API prefix used here bc I'm hosting this lambda at /services on apigw and it adds it automatically, but local does not
API_PREFIX=/services

# Environment here is the same as environment_tag and is used for database name "suffix"
ENVIRONMENT=Test