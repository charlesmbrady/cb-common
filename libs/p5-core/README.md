# p5-core

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build p5-core` to build the library.

## Running unit tests

Run `nx test p5-core` to execute the unit tests via [Jest](https://jestjs.io).
