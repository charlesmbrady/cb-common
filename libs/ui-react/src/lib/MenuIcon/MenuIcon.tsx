import styled from '@emotion/styled';

const StyledMenuIcon = styled.div`
  color: pink;
`;

export function MenuIcon() {
  return (
    <StyledMenuIcon>
      <h1>Welcome to MenuIcon!</h1>
    </StyledMenuIcon>
  );
}

export default MenuIcon;
