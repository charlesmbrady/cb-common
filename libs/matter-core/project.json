{
  "name": "matter-core",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/matter-core/src",
  "projectType": "library",
  "tags": ["js"],
  "targets": {
    "build": {
      "executor": "@nx/js:tsc",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/libs/matter-core",
        "main": "libs/matter-core/src/index.ts",
        "tsConfig": "libs/matter-core/tsconfig.lib.json",
        "assets": ["libs/matter-core/*.md"]
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "libs/matter-core/jest.config.ts"
      }
    }
  }
}
